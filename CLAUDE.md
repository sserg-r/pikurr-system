# CLAUDE.md

Инструкции для работы с этим репозиторием (PIKURR/REPIKURR — ETL и
доставка данных геоботанической оценки полей). Один раунд — одна
сессия (`prompts/SESSION_RESTART.md`); всё, что новой сессии нужно
знать, должно быть здесь или в `docs/round*.md`, не только в контексте
прошлой сессии.

## Не делать

- не менять `passes_quality()`, `segmentate.py`, пороги цветовой
  коррекции (`is_green`, `is_soil`, `is_water_blue`), модель,
  нормализацию входа;
- не менять математику `merge_imageset` (формулы сдвигов, паддинг, кроп);
- не менять схему БД ETL-контура;
- не переобучать и не предлагать переобучение;
- не удалять карантин раунда 16 и каталоги `_removed_`;
- не коммитить `.env`, `deliver.env`, пароли, хеши `basic_auth`,
  приватные ключи — проверять `git check-ignore -v` перед добавлением
  любого файла, похожего на конфигурацию с секретами;
- не записывать временные тайлы в `_pool/` или в папки листов;
- не переписывать историю git без явного запроса пользователя —
  решение о переписывании истории (`git filter-repo`/BFG, force-push)
  принимает пользователь, не Claude Code;
- не удалять стор `image_assessment_2024` в GeoServer и не менять
  состав годов на витрине без явного решения пользователя (round25:
  зафиксирован факт существования стора на несуществующий каталог, не
  устранён — решение связано с вопросом многолетности витрины);
- не подстраивать замеры под цифры прошлых раундов — каждый замер
  самостоятельный, с честным указанием источника при сравнении.

## Машины и доступ

**VPS (прод)** — `geobotany.of.by`, `158.160.237.90` (Yandex Cloud,
2 **долевых** (burstable) vCPU, ~6 ГБ RAM, прерываемая ВМ).
`ssh sgr@158.160.237.90`. **`sudo -n` работает без пароля** (проверено
round33: `reboot`, `journalctl -b`, правка `/etc/systemd/system/`,
`systemctl` — всё без запроса пароля). Каталог — `~/repikurr`.
Контейнеры: `pikurr_vps_{postgis,geoserver,react,caddy}`
(`docker compose -f docker-compose.vps.yml ...` — **обязательно** с
`-f`, иначе поднимется не тот файл и появится паразитный контейнер).
Секреты: `~/repikurr/.env` (пароли Postgres/GeoServer-админа, basic
auth Caddy), `~/repikurr/deliver.env` (доступ к БД и GeoServer для
`deliver.py`/`healthcheck.py`). Автоподъём стека при загрузке —
`pikurr-vps-stack.service` (systemd, `docker compose up -d`); сам
Docker тоже перезапускает контейнеры с `restart: unless-stopped`
независимо от этого юнита. Автозапуск САМОЙ ВМ (после
принудительного простоя прерываемой машины) — Yandex Cloud Function
(код и инструкция в `REPIKURR/cloud-function-autostart/`, статус
развёртывания не подтверждён этой сессией — нет API-доступа к Yandex
Cloud, см. "Открытые вопросы" ниже).

**Эмулятор/стенд `192.168.251.190`** (хост `user-MS-7E01`, 12-16 полных
vCPU) — совмещает ДВЕ роли на одной машине: `~/pikurr-system` — ETL
(сборка/тест пакетов, GPU-инференс, не относится к витрине), `~/repikurr`
— тестовый клон REPIKURR (фронтенд/GeoServer/PostGIS), безопасная
площадка для экспериментов, которые нельзя ставить на прод (полные
перезагрузки, `docker pause`, инъекция отказов). `ssh 192.168.251.190`
(пользователь `user`). **`sudo -n` работает без пароля** — обновлено
round33 (запись от 2026-09-14 в памяти ошибочно утверждала обратное;
права могли измениться, либо более ранняя проверка была неверной —
перепроверять фактом в начале раунда, не доверять старым записям
вслепую). Контейнеры REPIKURR: `pikurr_srv_{postgis,geoserver,react,nginx}`
(`docker compose -f docker-compose.server.yml ...`, обязательно с `-f`
по той же причине, что и на VPS). Секреты — `~/repikurr/.env`,
`~/repikurr/deliver.env`, тот же формат, что на VPS. **Мерить им нельзя**:
GeoServer готовится после перезагрузки на порядок быстрее, чем на VPS
(round33: ~15с против ~51-90с) — это машина для функциональных
проверок и ceiling-оценок, не для цифр производительности, которые
претендуют описывать прод.

**Координаторская машина** (где работает эта сессия Claude Code) —
`sudo -n` НЕ работает без пароля (проверено фактом round35:
`sudo -n true` → `sudo: требуется указать пароль`) — в отличие от VPS
и эмулятора, где `sudo -n` проходит без пароля (см. выше). Из-за этого,
например, `npx playwright install --with-deps` (round34, блок B) не
может доустановить системные библиотеки на координаторе — только сам
браузер (`npx playwright install chromium`, без `--with-deps`).

**Сеть**: с координаторской машины (где работает Claude Code) прямой
HTTP-доступ к `192.168.251.190` (порты 80/8090) может не проходить
(наблюдался стабильный `000`/timeout от `curl`, при этом `ssh` и `ping`
работали) — не путать с реальной недоступностью сервиса; проверять
через `ssh 192.168.251.190 curl ...` или через `claude-in-chrome`,
у которого сетевой путь до этого хоста, судя по всему, отдельный от
координаторской сессии bash (тоже не 100% надёжен — были ложные `502`
через браузер, не подтверждавшиеся прямым `curl` с самой машины).

## Ловушки, купленные ошибками

- **`docker compose` только с `-f <нужный файл>`** — без него на обеих
  машинах поднимается ДРУГОЙ дефолтный `docker-compose.yml`
  (например, ETL вместо REPIKURR), создавая паразитный
  неправильно названный контейнер и лишний том (round32). Правильный
  файл для существующего контейнера — `docker inspect
  <контейнер> --format '{{index .Config.Labels
  "com.docker.compose.project.config_files"}}'`.
- **`docker restart`/тег образа не заставляют контейнер подхватить
  новый образ** — контейнер привязан к ID образа на момент создания;
  после `docker tag ... :latest` нужен `docker compose -f <файл> up -d
  --force-recreate <сервис>` (round32).
- **На эмуляторе `nginx.local.conf` резолвит upstream `geoserver`
  СТАТИЧЕСКИ при собственном старте**, не лениво через Docker DNS —
  если `geoserver`-контейнер недоступен по имени в момент старта/рестарта
  nginx, nginx падает целиком (`host not found in upstream`), забирая
  с собой и раздачу React SPA (тот же nginx хендлит `/`). На проде
  Caddy так не делает (резолвит на каждый запрос) — риск специфичен
  для стенда (round33, найдено при инъекции отказа GeoServer, не
  исправлено — не было причиной реальной аварии).
- **REST `PUT` на GWC diskquota меняет только рантайм, никогда не
  пишет файл на диске** — причина найдена фактом в round34 (block D1):
  `PUT /geoserver/gwc/rest/diskquota.xml` сразу виден через `GET` того
  же URL, но `geoserver_data/gwc/geowebcache-diskquota.xml` на диске
  остаётся со старым значением байт-в-байт сразу после успешного PUT,
  ДО какого-либо рестарта — то есть дело не в «REST-настройка не
  переживает рестарт» (это была верная, но неполная формулировка
  round33), а в том, что REST для этого конкретного эндпоинта вообще
  не персистентен, только runtime. При старте GeoServer читает файл на
  диске и переопределяет рантайм-значение обратно к дефолту (20 GiB).
  **Устойчивое решение — писать сам файл напрямую**, не полагаться на
  REST: `REPIKURR/tools/gwc_diskquota_2gib.xml` (инструкция применения
  и отката — в комментарии файла; схема на диске отличается от
  REST-схемы, корневой элемент `<gwcQuotaConfiguration>`, не
  `<org.geowebcache.diskquota.DiskQuotaConfig>`). Проверено фактом
  рестартом на эмуляторе и на VPS (round34) — квота 2 GiB пережила
  `docker compose restart geoserver` на обеих машинах. Регистрация
  тайловых слоёв (grid, `metaWidthHeight`, `expireClients`) — тоже
  файловая и тоже переживает рестарт (не путать с этим пунктом, это
  было верно и раньше).
- **HTTP-проверка не исполняет JavaScript.** `curl`/`healthcheck.py`
  видят код 200 и корректный `index.html`, даже если весь React
  падает с необработанным исключением при рендере (round33: `MapView.jsx`
  использовал `const` до объявления — `ReferenceError` на каждой
  загрузке с момента деплоя round32, никто не заметил, потому что
  верификация была HTTP-level). Выкатка фронтенда не считается
  проверенной, пока не пройден дымовой сценарий в настоящем браузере
  (см. "Инструменты измерения").
- **`docker-compose depends_on` без `condition: service_healthy`
  управляет только порядком СОЗДАНИЯ контейнеров**, не ожиданием
  реальной готовности приложения внутри — GeoServer (JVM) после
  старта контейнера ещё ~15-90с (машинозависимо) не готов принимать
  запросы, а Caddy/nginx уже проксируют на него.
- **Ответ `200` с пустым/синтетическим телом — не успех.** WFS/WMS
  могут отдать `200` с `ServiceExceptionReport` (round24); Caddy
  `handle_response` без `copy_response` подменяет ЛЮБОЙ ответ апстрима
  (включая `404`/`500`) на пустой `200` (round30). Проверять код **и**
  тело, всегда.
- **Нагрузочный тест без проверки содержимого измеряет пустоту, не
  производительность** (round31, блок B): случайные bbox дают до 100%
  пустых тайлов, время рендера пустоты систематически ниже времени
  рендера данных. Использовать `docs/round32_assets/tiles_with_data.json`
  или явно замерять и показывать долю пустых/маленьких ответов.
- **На эмуляторе работает собственный `watchdog.py`**
  (`pikurr-watchdog.service`), перехватывающий пакеты из `~/repikurr/inbox/`
  — ручной параллельный запуск `deliver.py` с ним конкурирует за один
  и тот же пакет (см. `flock` в watchdog, round21 A4, но конкуренция за
  СМЫСЛ доставки — не за файл — всё ещё возможна).
- **Секрет БД в разных местах должен обновляться синхронно**: пароль
  роли `pikurr` и пароль администратора GeoServer живут одновременно в
  `deliver.env`, реальной роли PostgreSQL, `.env` (docker-compose) и
  датасторе `postgis_pikurr` внутри GeoServer (`geoserver.jceks`).
  Смена пароля БД без обновления всех четырёх — `ServiceExceptionReport`
  («password authentication failed») при полностью корректной
  остановке доставки (round25).
- **Не доверять старым записям памяти о правах доступа без
  перепроверки** — `sudo` на эмуляторе был недоступен на 2026-09-14 и
  доступен на 2026-09-23 (round33); что изменилось, не выяснялось.
- **Общий эндпоинт GWC (`/geoserver/gwc/service/wms`) требует имя слоя
  С ПРЕФИКСОМ workspace** (`pikurr:слой`), в отличие от обычного
  `/geoserver/pikurr/wms`, где workspace уже в пути и префикс
  необязателен (round34, блок A) — стоило рабочего растрового слоя на
  проде целый раунд: фронтенд слал `layers=image_assessment` без
  префикса, GWC отвечал `400 Unknown layer` на каждый тайл, а
  `healthcheck.py` этого не видел, т.к. проверял только
  `/geoserver/pikurr/wms` (см. "Критерий приёмки" и блок B round35 —
  проверки должны ходить по тем же URL, что фронтенд).

## Инструменты измерения

- **Набор тайлов с данными**: `docs/round32_assets/tiles_with_data.json`
  — 20 bbox по центроидам полей, каждый подтверждён фактом на непустой
  ответ (z=13). Использовать во всех последующих замерах вместо новых
  случайных bbox.
- **Дымовой сценарий браузера**: `REPIKURR/tools/smoke/smoke.mjs`
  (Playwright, round34, блок B) — `cd REPIKURR/tools/smoke && npm install
  && npx playwright install chromium && BASE_URL=<домен> node smoke.mjs`.
  Проходит по каждому элементу управления витрины (загрузка, год, район,
  включение/выключение слоя AI-оценки, клик по полю — попап с реальным
  содержимым, зум, сдвиг, клик в области без данных) с проверкой по
  содержимому на каждом шаге (не только «страница открылась»), выводит
  PASS/FAIL по шагам и код возврата 1 при любом провале. Также пишет
  `captured_gwc_urls.json` (round35, блок B1) — источник истины для
  `healthcheck.py` по реальным URL кэшируемых слоёв. **Выкатка
  фронтенда на бой не считается завершённой, пока этот сценарий не
  пройден целиком и результат не приложен к отчёту раунда** — HTTP-проверка
  ответов JavaScript не исполняет и заменой не является (урок раундов
  32-33, round34 A показал живой пример: `healthcheck.py` был зелёным,
  пока слой растра отдавал 400 на каждый тайл). На VPS это правило
  вшито в `REPIKURR/deploy_frontend_vps.sh` (round35, блок B3) — сам
  вызывает `smoke.mjs` в конце деплоя, не даёт считать раскатку
  успешной без зелёного прогона. `claude-in-chrome`
  (ручной прогон агентом) остаётся резервным способом при недоступности
  Node/Playwright в среде, но не кладётся в репозиторий как исполняемый
  артефакт — это интерактивный MCP-инструмент, а не автономный скрипт.
- **`REPIKURR/healthcheck.py`** — `--base-url <домен>`, `--json` для
  машиночитаемого вывода. Проверки: `wms_getmap`, `gwc_layer[...]`
  (round35, блок B1 — по каждому кэшируемому слою, URL БЕРЁТСЯ из
  `REPIKURR/tools/smoke/captured_gwc_urls.json`, перезаписываемого
  `smoke.mjs`, а не собирается вручную; проверено фактом на эмуляторе,
  что ломает `smoke.mjs` → красит эту проверку — round35 B2), `wfs_getfeature`,
  `error_path` (тело `/geoserver/nonexistent` непустое), `main_page`,
  `year_district_json`, `db_matches_static` (требует
  `FRONTEND_DB_PASSWORD` из `deliver.env` — без него эта ОДНА проверка
  красная, остальные валидны). При деплое ОТДЕЛЬНЫМ файлом (не всем
  деревом монорепо) — задать `TILES_WITH_DATA_PATH` и
  `CAPTURED_GWC_URLS_PATH`, иначе `gwc_layer[...]` красная с понятной
  причиной (файлы не найдены по относительному пути).
- **`REPIKURR/deploy_frontend_vps.sh`** (round35, блок B3) — раскатка
  react-образа на VPS (build → бэкап тега → save/scp/load → retag →
  force-recreate), сам обязательно прогоняет `smoke.mjs` против
  раскатанного домена в конце; красный прогон = ненулевой код возврата
  и печать команды отката, деплой НЕ считается завершённым — заменяет
  ручное следование инструкции, чтобы шаг со smoke.mjs нельзя было
  случайно пропустить (см. "Ловушки").
- **`REPIKURR/check_delivery_window.sh`** — опрос окна недоступности
  во время доставки. Env: `BASE_URL`, `OUT`, `DURATION_S`,
  `POLL_INTERVAL_S`, `CURL_TIMEOUT_S` (5, round32 — обосновано худшим
  наблюдённым успешным ответом), `SLOW_THRESHOLD_S` (1.5 — отделяет
  `SLOW` от `OK`, не путать с `NORESPONSE`).
- **`REPIKURR/external_healthcheck.sh`** + `REPIKURR/systemd/pikurr-external-healthcheck.{service,timer,logrotate}`
  (round33) — регулярный внешний прогон `healthcheck.py` каждые 5
  минут С ЭМУЛЯТОРА против прод-домена (не с самой VPS — иначе отказ
  VPS остановит и мониторинг за неё); `db_matches_static` исключена из
  вердикта (нет доступа к секрету VPS с эмулятора). Лог с ротацией —
  `~/repikurr/external_healthcheck.log` на эмуляторе.
- **`REPIKURR/tools/k6_loadtest.js`** (round33: перенесён из `/tmp`) —
  реалистичный профиль: 20 bbox с данными из `tiles_with_data.json`,
  реальный путь фронтенда (`/geoserver/gwc/service/wms&tiled=true`).
  Env: `BASE_URL`, `VUS`, `REPS_PER_VU`, `TILES_PATH`. Более ранние
  профили (round28-31, случайные bbox) — НЕ переносились в репозиторий
  специально: они измеряли то, что описано в разделе "Ловушки" выше
  (пустоту, не производительность), не являются образцом для
  повторного использования.
- **`REPIKURR/tools/gwc_diskquota_2gib.xml`** — не измерение, а
  восстановительный инструмент: файл конфигурации квоты диска GWC для
  прямой записи в `geoserver_data/gwc/geowebcache-diskquota.xml`
  (не через REST — см. "Ловушки"), с раунда 34 устойчив к рестарту.
- **`REPIKURR/tools/gwc_metatile_experiment.py`** (round34, блок C) —
  сравнение суммарного времени отрисовки экрана из 12 тайлов при
  разных `metaWidthHeight` (4×4/2×2/1×1) на свежих областях. **Только
  эмулятор** — многократно переключает конфигурацию слоя и генерирует
  MISS-нагрузку намеренно. Использованные результаты — в
  `docs/round34-raster-gwc.md`, C2; абсолютные цифры с эмулятора не
  переносить на VPS (см. "Правила замеров").
- **`REPIKURR/tools/tile_math.py`** (round35) — единственная версия
  математики lon/lat↔тайл↔bbox EPSG:3857 (переизобреталась вручную
  минимум дважды до этого — round32, round34). `python3 tile_math.py
  --self-check` сверяет с `docs/round32_assets/tiles_with_data.json`.
  Содержит `tiles_overlap()` — программная проверка непересекаемости
  областей эксперимента (не на глаз). round36 добавил
  `tile_range_for_bbox_3857(bbox, z)` — диапазон тайлов, пересекающих
  bbox (используется в `gwc_http_seed.py` и для сверки цели GWC-засева
  с реальным числом файлов на диске, `docs/round36-seeding-fix.md`,
  блок D); использует `math.floor(x / tile_size)`, НЕ `x // tile_size`
  — на координатах точно на границе тайла (обычный случай для bbox
  слоя) эти два способа дают разный результат из-за плавающей точки,
  сверкой с реальными файлами на диске подтверждён верным первый.
- **`REPIKURR/tools/gwc_http_seed.py`** (round36, блок B) — прогрев
  кэша обычными HTTP GetMap-запросами тем же эндпоинтом, что фронтенд
  (`/geoserver/gwc/service/wms&tiled=true`), N потоками, с NDJSON-логом
  по каждому ответу (код, размер, `geowebcache-cache-result`),
  `--resume` и корректным завершением по сигналу. Независимый от
  внутреннего засева GWC способ "видеть каждый ответ" — на случай, если
  версия/конфигурация GWC снова станет вести себя непрозрачно.
- **`REPIKURR/tools/verify_tiles_have_data.py`** (round35) —
  консолидация проверки «правило непустых тайлов» по набору bbox из
  `tiles_with_data.json`, через GWC или обычный WMS, с долей
  пустых/маленьких ответов и предупреждением, если выше 20%.
- **`REPIKURR/tools/analyze_zoom_usage.py`** (round35, блок C1) —
  разбирает `tools/smoke/all_tile_requests.json` (пишет `smoke.mjs`)
  в распределение зумов, которые реально запрашивает фронтенд за
  прогон дымового сценария.
- **`REPIKURR/deploy_frontend_vps.sh`** (round35, блок B3) — см. запись
  про `REPIKURR/healthcheck.py` выше.

## Правила замеров

- Цифры со стенда/эмулятора (`192.168.251.190`), VPS
  (`geobotany.of.by`) не смешивать и не переносить друг на друга;
  каждый замер подписывать машиной. Разница — на порядок и больше
  (round24/25: `REFRESH MATERIALIZED VIEW` — единицы секунд на стенде,
  десятки на VPS; round33: готовность GeoServer после перезагрузки —
  ~15с на эмуляторе, ~51-90с на VPS).
- Минимум 3 повтора, порядок перемешан, состояние кэша (холодный/тёплый)
  указано явно.
- Разброс на VPS до 3× и выше — свойство burstable-машины, не аномалия
  (не пытаться объяснять или устранять сам разброс).
- **Правило непустых тайлов**: доля ответов < 2 КБ указывается в
  КАЖДОМ замере скорости витрины; если она выше 20% — замер не
  считается валидным выводом (round32, после наблюдения 42-100% пустых
  тайлов на случайных bbox в round28-31).
- Не подстраивать новые замеры под цифры прошлых раундов — каждый
  самостоятелен, сравнение — с честным указанием источника.

## Критерий приёмки — по содержимому, не по коду ответа

WFS/WMS GeoServer могут отдавать `HTTP 200` с телом
`ServiceExceptionReport` (round24, задача 2) — протокольная
особенность старых версий WFS/WMS, не баг GeoServer, но означает, что
проверка по HTTP-коду слепа к реальным сбоям. Любая проверка
работоспособности витрины должна разбирать тело ответа
(`REPIKURR/healthcheck.py`, round25, блок B5), а не только код.

Симметричный случай на стороне Caddy (round30, блок C, найдено
проверкой ТЕЛА `/geoserver/nonexistent`, а не только заголовков — до
этого баг жил в проде незамеченным целый раунд): директива
`handle_response` внутри `reverse_proxy` по умолчанию НЕ пробрасывает
исходный ответ апстрима дальше — без явного `copy_response` первой
строкой внутри блока `handle_response` статус и тело подменяются
пустым синтетическим `200 Content-Length: 0`, даже если GeoServer
реально ответил `404`/`500`/`ServiceExceptionReport`. Не доверять
`handle_response` без проверенного факта, что `copy_response`
присутствует.

**Тот же принцип на уровне фронтенда (round33)**: HTTP-код 200 и
корректный `index.html` ничего не говорят об успешности выполнения JS
на клиенте — необработанное исключение при рендере (без React Error
Boundary) размонтирует всё дерево React, оставляя `#root` пустым.
Верификация фронтенда после серьёзных правок обязана включать реальный
браузер (см. "Инструменты измерения"), не только HTTP-уровневые
проверки. `ErrorBoundary` (`REPIKURR/repikurr/src/components/ErrorBoundary.jsx`,
round33) не предотвращает баги, но не даёт будущему необработанному
исключению снова превращаться в тишину.

## Текущее состояние системы

- **Схема БД**: `SCHEMA_VERSION = 3` (`REPIKURR/deliver.py`). Три
  материализованных представления: `assessment_ready`,
  `assessment_ready_latest`, `levelsagg_ready` (все должны быть
  `ispopulated = t`).
- **GWC-кэш**: включён (round32) для `pikurr:fields_latest` (векторный)
  и `pikurr:image_assessment` (текущий растр), grid `EPSG:900913`
  (=Web Mercator, соответствует тайлам Leaflet), `metaWidthHeight`
  4×4 (round34, блок C: увеличение суммарного времени отрисовки экрана
  при БОЛЬШЕМ метатайле подтверждено фактом на эмуляторе для холодных
  областей — решено НЕ менять на VPS, обоснование — round34 C3),
  `expireCache=0` (не истекает сам, только явный truncate),
  `expireClients=86400` (браузерный `Cache-Control`). Квота диска —
  2 GiB, персистентна через рестарт GeoServer с round34 (файл
  `geoserver_data/gwc/geowebcache-diskquota.xml` пишется напрямую, не
  через REST — см. "Ловушки"). Инвалидация — `deliver.py` вызывает
  `_truncate_gwc_layer_if_cached()` для векторных слоёв сразу после
  `refresh_materialized_view()` и для растровых — внутри
  `_reload_one_store()`.
- **Прогрев кэша (seed) при доставке** (round35, блок C; причина
  "зависания" найдена и устранена round36) — реализован (`deliver.py`,
  `seed_gwc_cache()`/`_seed_gwc_layer()`, шаг после `reload_geoserver`),
  **включён по умолчанию** (`GWC_SEED_ENABLED=true`). round35 приняла
  зависание на ~50% за реальную проблему GWC — round36 (декомпиляция
  GWC 1.24.2 + 13/13 воспроизводимых прогонов на эмуляторе и VPS)
  установила: реального зависания не было, это баг в САМОМ `deliver.py`
  — при `threadCount>1` каждый поток репортит ОДНО И ТО ЖЕ полное
  значение `tilesTotal` (не свою долю), а код суммировал `total` по
  потокам, искусственно удваивая отображаемую цель. Пустой список
  задач GWC — легитимное завершение (`RUNNING→DONE`, задача вычищается
  `drain()`), проверено по содержимому (диск + HIT на контрольных
  тайлах) в 13/13 прогонов. Подробности — `docs/round36-seeding-fix.md`.
  `GWC_SEED_MAX_WAIT_S` поднят `300→420с` — полный диапазон z9-14 на
  VPS занимает вплотную к прежнему пределу (299,76с, факт round36).
- **Эндпоинты**: кэшируемые слои (`fields_latest`, текущий
  `image_assessment`) — через `/geoserver/gwc/service/wms` с
  `tiled=true` (стандартный `/geoserver/pikurr/wms` НЕ включает GWC
  даже с `tiled=true` — проверено фактом, round32). Историчные слои
  (`fields` с `CQL_FILTER=year=...`, `image_assessment_<год>`) — через
  обычный `/geoserver/pikurr/wms` напрямую (GWC для них не
  регистрировался — не входило в объём round32). **Важно** (round34,
  блок A): `/geoserver/gwc/service/wms` не виртуализован по workspace,
  параметр `layers` там ОБЯЗАН быть полным именем (`pikurr:слой`);
  `/geoserver/pikurr/wms` принимает оба варианта, т.к. workspace уже в
  пути — фронтенд (`MapView.jsx`) теперь передаёт полное имя в обоих
  случаях (`rasterFqName`/`vectorLayer`).
- **Фронтенд**: `ErrorBoundary` на верхнем уровне (`main.jsx`), баннеры
  деградации при недоступности `year_district.json` и при массовых
  ошибках тайлов WMS (`MapView.jsx`, `tileerror`), атрибуция карты —
  только сторонние источники (OSM/Esri), без ссылки на саму Leaflet
  (round33). Выкатка на бой обязана пройти
  `REPIKURR/tools/smoke/smoke.mjs` (round34, блок B) — см. "Инструменты
  измерения".
- **Caddy**: `lb_try_duration=60s` (round34, блок D2 — 30с оказались
  мало для реального прод-рестарта GeoServer, 3 прогона на VPS дали
  34.9-36.9с окна полной недоступности, все выше 30с).
- **Внешний мониторинг**: systemd timer на эмуляторе, `healthcheck.py`
  каждые 5 минут против прод-домена, лог с ротацией (round33). Способ
  уведомления пользователя (Telegram-бот и другие варианты) —
  спроектирован, не внедрён.

### Открытые вопросы (сознательно не закрыты)

- **Автозапуск ВМ (Cloud Function)** — код и инструкция готовы с
  round23, фактический статус развёртывания не подтверждён: у Claude
  Code нет API-доступа к Yandex Cloud. Проверять по логам функции в
  консоли (см. `docs/round33-reboot-incident.md`, блок B4).
- **`image_assessment_2024` — стор на несуществующий каталог** (round25)
  — не устранено, решение зависит от вопроса многолетности витрины,
  который пользователь ещё не принял.
- **53-76× vs 11-14× расхождение в оценке ускорения** (round31, блок A)
  — объявлено невосстановимым, дальнейшие раунды считают от
  собственных свежих замеров, не пытаются согласовать со старым
  расхождением.
- **k6 VU=3, один прогон из 9 — 30% таймаутов** (round32) — гипотеза
  (control-flow GeoServer при 60 одновременных запросах с одного IP)
  не подтверждена отдельным экспериментом; `controlflow.properties` не
  трогать без явного решения пользователя.
- **nginx на эмуляторе статически резолвит upstream `geoserver`**
  (round33) — риск для стенда, не для прода (Caddy резолвит лениво);
  не исправлено (вне причины аварии round33, некритично для тестового
  контура).
- **`lb_try_duration=60s` (round34) — не пересмотрено под рост
  нагрузки**: запас над сегодняшним худшим прод-замером (36.9с), но
  цена — клиент в окне рестарта GeoServer теперь может висеть до 60с,
  занимая соединение Caddy; при заметном росте одновременного трафика
  стоит заменить на активный health-check + управляемый отказ вместо
  простого таймаута (round34, блок D2) — не сделано, текущая нагрузка
  (единицы пользователей) не требует.
- **Прогрев (seed) кэша GWC на VPS не проверен через полноценную
  доставку `deliver.py`** (round36, блок E) — сама механика засева
  проверена напрямую (REST-вызовы тем же протоколом на боевом
  GeoServer, 4 успешных прогона, включая полный z9-14), но НЕ через
  оркестрацию `deliver.py` целиком (в `inbox/` не было тестового
  ZIP-пакета). Флаг включён по умолчанию — при следующей настоящей
  доставке на VPS стоит явно проверить шаг `seed_gwc_cache` в статусе
  доставки и здоровье как обычно (`healthcheck.py` + `smoke.mjs`).
- **Возможная конкуренция потоков при последовательном засеве двух
  слоёв** (round36, побочное наблюдение, не по одной из трёх гипотез
  ТЗ): если засев одного слоя не укладывается в `GWC_SEED_MAX_WAIT_S`,
  `deliver.py` перестаёт его ждать и переходит к следующему слою — но
  GWC продолжает досеивать первый слой в фоне, и оба слоя временно
  засеиваются одновременно (2+2 потока вместо 2), что замедляет второй
  слой. Наблюдалось один раз на эмуляторе под посторонней нагрузкой от
  параллельных экспериментов этого же раунда — не воспроизводилось в
  изолированных условиях (13/13 чистых прогонов уложились в разы
  быстрее нового предела 420с). Не исправлено — `GWC_SEED_MAX_WAIT_S`
  с запасом снижает вероятность, но не устраняет саму возможность
  каскада при реальной аномальной нагрузке VPS.
