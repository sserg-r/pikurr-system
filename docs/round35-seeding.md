# Раунд 35: передача знаний, проверки по реальным URL, прогрев кэша

ТЗ: `prompts/PROMPT_seeding_round35.md`.

---

## Блок A. Передача знаний в файлы

### A1. Инструменты из `/tmp` — в репозиторий

Обследованы все файлы в `/tmp` на координаторе (не только раунда 34 —
всё, что накопилось с раунда 21 и позже).

**Перенесено/консолидировано**:
- `REPIKURR/tools/tile_math.py` — математика lon/lat↔тайл↔bbox EPSG:3857.
  Обнаружено, что эта математика независимо переизобреталась минимум
  дважды: `/tmp/round32_tile_math.py` (не коммитился) и инлайн-python
  внутри Bash-эвристик round34 (A1.4, C1, C2). Консолидирована в один
  модуль с `--self-check`, сверенным с
  `docs/round32_assets/tiles_with_data.json` — **20/20 совпало**.
- `REPIKURR/tools/verify_tiles_have_data.py` — консолидация паттерна
  «правило непустых тайлов» (`/tmp/round32_verify_tiles.py` +
  повторявшиеся curl-циклы round34 A1.4/C1). Проверено запуском против
  прода: `0/20 пустых (< 2048 байт) — 0%`, заодно подтвердило, что
  растровый слой на проде по-прежнему в порядке.

**Удалено из `/tmp`** (устарело или уже перенесено в более раннем
раунде — содержимое либо дублирует committed-файлы, либо было
одноразовым логом без ссылки из отчёта): файлы раунда 32
(`round32_tile_math.py`, `round32_verify_tiles.py`,
`round32_candidate_tiles.json`, `round32_verified_tiles.json` и ещё ~15
вспомогательных txt/json того же раунда — результат уже зафиксирован в
`docs/round32_assets/tiles_with_data.json` и `docs/round32-tile-cost.md`),
файлы раунда 34 (скрипт эксперимента с метатайлами и промежуточные
curl-логи — уже перенесены в `REPIKURR/tools/gwc_metatile_experiment.py`
и `docs/round34-raster-gwc.md` этим же раундом), три React-образа
(`*.tar.gz`, по 26 МБ каждый — промежуточные артефакты `docker save`,
уже доставлены и раскатаны, хранить нет смысла).

**Оставлено намеренно** (с причиной):
- `/tmp/gpu_smoke_test.py`, `/tmp/gpu_smoke_test_z18.py` — GPU-smoke
  тесты обучения (`PIKURR/src/fs/training/...`) — это ETL-сторона
  проекта (`pikurr-system`), не REPIKURR/витрина, вне объёма раундов
  28-34 и этого раунда; ни один отчёт REPIKURR-раундов на них не
  ссылается.
- `/tmp/mincore_check.py` — проверка резидентности `.tif`-файлов в
  page cache ОС, тоже ETL-сторона (работает с растровыми масками
  обучения, не с GWC/витриной), не референсится ни одним раундом.
- `/tmp/test_poll_status.py` — черновой mock-харнесс для
  `PIKURR/src/tasks/push.py::PushTask`. Похоже связан с правкой round30
  («push.py ждёт итоговый статус, не промежуточный»), но сама правка
  была верифицирована живой доставкой (round29/30), не этим скриптом;
  формального тест-сьюта для `PIKURR/src/tasks` в репозитории нет — не
  оформлять один одноразовый скрипт в тест-сьют без остальных.
- Каталог `/tmp/claude-1000/.../scratchpad/round33/*.txt` (логи
  инцидента: `journal_docker.txt`, `log_geoserver_full_tail.txt` и
  т.д.) — сырые логи, из которых уже написан
  `docs/round33-reboot-incident.md`; сами логи не являются
  самостоятельным инструментом и лежат в scratchpad другой сессии, не
  в общем `/tmp` — не трогать.

### A2. Аудит CLAUDE.md

Проверено по чеклисту ТЗ, найдено и дописано:
- **Отсутствовал статус `sudo` на координаторе** — добавлен факт:
  `sudo -n true` → требует пароль (не проходит без интерактива), в
  явном контрасте с VPS/эмулятором, где `sudo -n` проходит.
- **Требование workspace-префикса для `/geoserver/gwc/service/wms`**
  было только в разделе "Текущее состояние", отсутствовало как
  отдельный пункт "Ловушки" (хотя именно ловушкой и было — round34 A) —
  добавлено туда же с явной ссылкой на цену ошибки (сломанный слой на
  проде весь раунд).

Остальные пункты чеклиста (машины и доступ, `docker compose -f`,
параллельный watchdog, REST не пишет на диск, HTTP-проверка JS, `200` с
пустым телом, инструменты измерения с путями, правила замеров, текущее
состояние системы, открытые вопросы) — уже присутствовали после
раунда 34, актуальны, не требовали правок кроме двух выше.

Итоговый файл `CLAUDE.md` — см. корень репозитория (343→360 строк
после правок этого блока, полный текст не дублируется здесь, чтобы не
рассинхронизироваться с живым файлом).

### A3. Чистота

`git status --porcelain` — пуст после коммита этого блока (проверено
после `git push`, см. историю коммитов).

---
