# Раунд 26: фиксация работы в git, предохранители

См. `prompts/PROMPT_repo_and_guards_round26.md`. Раунд из двух блоков:
A (сохранность работы) — целиком до блока B (предохранители).

**Решения пользователя, принятые до раунда**: репозиторий остаётся
публичным, история не переписывается; стор `image_assessment_2024`
убирается с витрины, данные 2024 года в проект не добавляются.

**Правило замеров**: цифры со стенда `192.168.251.190` (12-16 vCPU) и с
VPS `158.160.237.90` (2 vCPU, ~50% гарантированной доли) не смешиваются
— каждый замер ниже подписан машиной.

**Особенность инфраструктуры сессии**: часть операций (чтение/хэширование
файлов с секретами, `DROP`/деструктивные SQL на боевых таблицах,
удаление GeoServer-стора на VPS) блокировалась классификатором
auto-mode Claude Code как `Credential Exploration`/`Modify Shared
Resources`/`Production Deploy`. Точечно разрешить это через
`autoMode.allow` в настройках сессии сам ассистент не может
(`Self-Modification`) — пользователь добавил правило вручную; там, где
это не помогло (сами SQL/REST-операции на проде), команды выполнял
пользователь лично, ассистент готовил их текст и разбирал результат.

---

## Блок A. Сохранность работы

### A1. Проверка на действующие секреты перед коммитом

**Сделано**: собран список действующих секретов (без значений) — пароль
роли `pikurr` в PostgreSQL (стенд/VPS), пароль admin GeoServer
(стенд/VPS), хеш `basic_auth` Caddy, ключ доставки SSH. Для каждого
получен SHA-256 (первые 12 симв.) напрямую от пользователя (команды
готовил ассистент, значения никогда не показывались в открытом виде):

| Секрет | Стенд | VPS |
|---|---|---|
| Пароль роли `pikurr` (.env / deliver.env) | `3e5d49cbb182` (оба файла совпадают) | `d1cd6c74e0f3` |
| Пароль admin GeoServer (.env / deliver.env) | `2d16e4344498` (оба файла совпадают) | `02857a5d9486` |
| `GEOSERVER_BASIC_AUTH_HASH` | — | длина 63 символа (bcrypt) |

Grep по всем 44 файлам, готовящимся к коммиту (`git add -n`, проверено
`git check-ignore -v` — ни один не игнорируется, ни один секретный файл
из списка не включён), на литеральные присвоения
`PASSWORD|SECRET|_HASH|API_KEY` — **ноль совпадений с действующими
значениями** (только `${VAR:?...}`-плейсхолдеры в compose-файлах и уже
известные исторические утёкшие значения `pikurr`/`geoserver` в
round24/25-отчётах — оставлены по решению пользователя из преамбулы
этого ТЗ).

**Находка** (не была известна ни в одном отчёте до этого раунда):
`REPIKURR/geoserver_data/security/usergroup/default/users.xml.orig` —
новый, ещё не закоммиченный файл, содержащий **реальный digest пароля
admin** GeoServer. `.gitignore` запрещал по точному имени только
`users.xml`, не `users.xml.orig` — оба `.orig`-файла (второй,
`roles.xml.orig`, безвреден сам по себе) попали бы в публичный
репозиторий. Исправлено — добавлены в `.gitignore` до A2.

**Подтверждено**: A1 выполнена, находка устранена до пуша.

### A2. Зафиксировать работу раундов 17-25 в git и запушить

**Сделано**: 44 файла разбиты на 6 осмысленных коммитов —
ETL (`676f880`), доставка/атомарность (`3e4874f`), фронтенд/витрина
(`ab35d52`), инфраструктура/compose (`02474fb`), Cloud Function
(`1e3d239`), документация (`3b116c4`). `.env`, `deliver.env`,
`geoserver_data/security/**` не коммитились (проверено `check-ignore`
до `git add` каждой группы). Запушено в `origin/main`
(`908b031..3b116c4`).

**Проверка фактом**:
- `git status --porcelain` — пуст.
- Чистый клон `origin/main` (`/tmp/round26_clean_clone`) собирает
  React (`npm install && npm run build`) без ошибок; в бандле
  `dist/assets/index-*.js` **есть** обращение к
  `/static/year_district.json` (1 совпадение) и **нет** старого
  WFS-запроса `propertyName=year,nr_user` (0 совпадений) — боевой
  фронтенд теперь совпадает с тем, что зафиксировано в `HEAD`.

**Подтверждено** полностью.

### A3. Воспроизводимость

**Сделано**: пройден `docs/deploy-repikurr-vps.md` от git+пакета,
ничего не создавалось и не менялось на серверах кроме уже выполненного
в A1/A2. Список того, чего не хватает для восстановления с нуля:

| Чего нет в репозитории | Где физически лежит | Последствие |
|---|---|---|
| `.env` (стенд, VPS) | только на каждом хосте, независимо | раздел 2 инструкции требует сгенерировать новые значения — не восстановление той же идентичности, а новое развёртывание |
| `deliver.env` (стенд) | только на стенде | то же |
| `geoserver_data/security/**` (мастер-пароль, `jceks`, `users.xml`) | только на каждом хосте, разные независимые копии | датасторы, зашифрованные старым мастер-паролем, придётся перенастраивать вручную |
| Приватный SSH-ключ доставки `pikurr_delivery` | только на стенде, никогда не в git | канал доставки нужно пересоздавать заново |
| Запись в `authorized_keys` на VPS (`command=...dispatch.sh`) | только на VPS, не версионируется | восстанавливается вручную |
| Данные PostGIS (`agrifields`/`razgrafka`/`assessment`) | не в git, только через реальный пакет | восстанавливается пакетом — **проверено фактом**: на момент раунда на стенде реально существовали 4 подходящих пакета (`/mnt/nfsdata/PIKURR/outputs/dist/pikurr_update_2025_*.zip`), но это не версионируется и подчиняется retention (30 дней по умолчанию) |

**Побочная находка** (важна, не была известна ни в одном отчёте до
этого раунда): `REPIKURR/geoserver_data/workspaces/pikurr/postgis_pikurr/datastore.xml`
версионируется в git с round22, но содержит **устаревшую** запись
пароля (`plain:pikurr` — уже известное утёкшее значение, хеш `777a7913`,
не активное сейчас нигде, поэтому по правилу проекта не вычищалось).
Реальный файл на стенде хранит `crypt2:...` (зашифровано мастер-паролем)
— **весь снапшот `geoserver_data/` в git (кроме `security/`) устарел**
относительно боевого состояния после более поздних изменений датастора
(round25, блок B2). Восстановление из репозитория дало бы рабочие
определения слоёв, но с нерабочей записью пароля датастора, требующей
ручного исправления после разворачивания.

**Вывод**: тезис «резервное копирование VPS не нужно, всё
воспроизводимо из пакета и репозитория» — **опровергнут в узком
смысле** (полностью автоматически новый инстанс не поднимется — нужны
сгенерированные вручную секреты и синхронизация датастора GeoServer),
**подтверждён в широком** (ничего необратимо не теряется, только часы
работы вместо минут, как и заявлено в шапке `deploy-repikurr-vps.md`).

Пользователь сделал снимок диска VPS в Yandex Cloud (шаги
сформулированы ассистентом, выполнено пользователем) — снижает и этот
остаточный риск.

### A4. Удаление стора `image_assessment_2024`

**Сделано**: перед удалением проверено REST — на слой `image_assessment_2024`
не ссылались ни группы слоёв (`{"layerGroups":""}`, пусто), ни GWC-кэш
(`[]`, пусто), ни стили (проверены стили workspace `pikurr` — только
`agrifields1`/`raster_vegetation`, общие, не относятся конкретно к 2024).
Удалено на VPS через REST (`DELETE .../coveragestores/image_assessment_2024?recurse=true`
→ `200`), `reload` → `200`. Убран и из версионированного
`geoserver_data/workspaces/pikurr/image_assessment_2024/` (коммит
`d49eed9`), чтобы не вернуться при следующем развёртывании из
репозитория. `data/geodata/2024/` на стенде не тронут.

**Проверка по содержимому** (VPS, `https://geobotany.of.by`):
- WMS 2025: `HTTP 200`, `PNG image data, 256 x 256` — реальный растр.
- Главная страница: `HTTP 200`.
- `year_district.json`: `{"years": [2025], ...}` — не содержал 2024 и
  раньше, не мог быть затронут удалением.
- `healthcheck.py --json`: 4 из 5 зелёные (`wms_getmap`, `wfs_getfeature`,
  `main_page`, `year_district_json`); пятая (`db_matches_static`) не
  прошла по ожидаемой причине — запуск без `FRONTEND_DB_PASSWORD` в
  окружении этой машины, не дефект от удаления.

**Подтверждено** полностью.

---

## Блок B. Предохранители

### B1. Проверка объёма данных внутри транзакции подмены

**Сделано**: `_swap_staged_tables()` в `deliver.py` теперь считает число
строк в каждой `_stage`-таблице и в соответствующей боевой **до**
`TRUNCATE`, внутри той же транзакции (`DO $$ ... RAISE EXCEPTION ... $$`
перед `TRUNCATE`, откатывает всю транзакцию при срабатывании). Порог —
`--shrink-threshold`, по умолчанию **10%** (обоснование: на порядок
больше обычной вариации между реальными доставками — единицы объектов
на ~59000, доли процента, — но заметно меньше катастрофической потери).
Флаг `--allow-shrink` отключает проверку для легитимной массовой
правки.

**Проверка фактом на стенде, три сценария** (реальный производственный
пакет `pikurr_update_2025_2026-09-18_05-21.zip`, скопированный и
модифицированный на месте):

| Сценарий | Ожидание | Статус-файл |
|---|---|---|
| Нормальный пакет | проходит | `{"ok": true, ...}`, healthcheck зелёный |
| `agrifields` опустошён (0 строк) | отказ, данные целы | `{"ok": false, "error": "...слой 'agrifields' — _stage пуст. _stage=0 строк, боевая=59209 строк..."}` |
| `agrifields` усечён на 30% (порог 10%) | отказ, данные целы | `{"ok": false, "error": "...shrink_over_threshold... _stage=41447 строк, боевая=59209 строк..."}` |

Боевая таблица `agrifields`: **59209 строк до и после** в обоих
сценариях отказа (проверено `SELECT count(*)` до/после). Витрина
(`WFS GetFeature`) отдавала реальные данные во время и после отказа —
не была затронута.

**Подтверждено** полностью.

### B2. Кто ещё читает боевые таблицы напрямую

**Сделано**: перечислены все featuretype workspace `pikurr` и их
`virtualTable` SQL:

| featuretype | источник |
|---|---|
| `fields` | `SELECT * FROM assessment_ready` (materialized view) |
| `fields_latest` | `SELECT * FROM assessment_ready_latest` (view над materialized) |
| `levelsagg` | `SELECT DISTINCT usname, usern_co, left(usern_co,4) FROM agrifields` — **напрямую боевая таблица** |

**Контролируемый эксперимент на стенде** (транзакция с открытым
`TRUNCATE agrifields, razgrafka, assessment` удержана 15с через
`pg_sleep`, во время удержания — прямой опрос всех трёх слоёв):

- `fields`: `HTTP 200`, 28.6мс — не блокируется.
- `fields_latest`: `HTTP 200`, 77.8мс — не блокируется.
- `levelsagg`: таймаут (`curl --max-time 8` истёк) — **блокируется
  полностью** на всё время удержания `ACCESS EXCLUSIVE`-лока.

**Длительность самой транзакции подмены на VPS** (боевой объём — 59209
+ 21280 + 55784 строк, метод — создание `_stage` как копии боевых,
`TRUNCATE`+`INSERT` обратно теми же данными, безопасно и обратимо):
**14.581 секунды** (VPS, один прогон, замерено пользователем; целостность
проверена — 59209 строк до и после).

**Вывод**: на каждой реальной доставке на VPS слой `pikurr:levelsagg`
(список землепользователей в фильтрах фронтенда) становится недоступен
примерно на **14.6 секунды** (число только для VPS). `fields`/`fields_latest`
(карта, статистика) не затронуты вовсе. ТЗ не требовало исправления —
находка зафиксирована для решения пользователя.

**Подтверждено** полностью, не исправлялось (вне scope B2).

### B3. Диагностика оставшегося ~1% сбоев

**Сделано**: опрошена витрина **вне доставки** — 1050 запросов (WFS +
WMS + главная страница, по ~350 циклов), с `192.168.251.190` как
внешнего хоста относительно VPS (методика round25 B1/B3), сохранены
для каждой аномалии HTTP-код, заголовки, первые 200 байт тела, время.

**Первая попытка опроса была методологически испорчена** (честно
зафиксировано): опрос сначала запускался с локальной машины ассистента,
которая идёт через sandbox-прокси (`https_proxy=127.0.0.1:8080`) —
единственная «аномалия» там была ошибкой самого прокси-туннеля
(`503`), не сбоем VPS. Переделано с `192.168.251.190`.

**Результат** (2 аномалии из 1050 = 0.19%):
1. `Temporary failure in name resolution` — сбой DNS-резолвера **на
   самом стенде** (клиентская сторона), не относится к VPS.
2. Таймаут WFS (10.97с при лимите 10с, `08:43:56 UTC`). Сопоставлено с:
   - `docker logs pikurr_vps_geoserver`: разрыв **12 секунд** без единой
     обработанной заявки control-flow (`08:43:44`→`08:43:56`);
   - `top` на VPS в этот момент: JVM GeoServer — **109% CPU**;
   - `iostat -x` на VPS: скачок `%steal` до **3.48%** (признак
     прерываемой ВМ — гипервизор забрал CPU-время), диск почти
     простаивает (`r/s=0, w/s=0`) — не дисковая причина.

**Вывод**: оставшиеся редкие сбои — **свойство ВМ** (прерываемая
burstable 2-vCPU + пауза JVM GeoServer под конкурентным throttling'ом),
не дефект `deliver.py`/атомарности/схемы. Доля (0.19% вне доставки)
ниже, чем 1.1% из round25 B3 (во время доставки) — подтверждает, что
именно нагрузка от доставки (копирование 912 растров) поднимает
частоту сбоев, а не скрытый баг в атомарности. **Не исправлялось**, как
и требовало ТЗ.

### B4. Негативный тест `healthcheck.py`

**Сделано**: на стенде каждая из 5 проверок доведена до красного
состояния индивидуально, код возврата и вывод показаны, стенд возвращён
в исходное состояние после каждого теста:

| Тест | Как сломано | Результат | Восстановлено |
|---|---|---|---|
| WMS не PNG | `coverageStore.enabled=false` через REST | `wms_getmap: false` (`HTTP 200, ServiceExceptionReport` — не сырые не-PNG байты, честно отмечено расхождение с буквальной формулировкой), код `1` | ✅, `ok: true` |
| WFS `ServiceExceptionReport` при `HTTP 200` | `featureType.enabled=false` (слой `fields`) | `wfs_getfeature: false` (`HTTP 200`), код `1` | ✅ |
| `year_district.json` недоступен | файл временно перемещён | `HTTP 404`, `year_district_json: false` + `db_matches_static: false` (каскадом), код `1` | ✅ |
| `year_district.json` не совпадает с БД | один район подменён на `9999` | `db_matches_static: false`, точная диагностика (`отсутствует в файле: {(2025, '2208')}; лишнее: {(2025, '9999')}`), код `1` | ✅ |
| Главная страница недоступна | `docker stop pikurr_srv_react` | `HTTP 502`, `main_page: false`, код `1` | ✅ |

**Побочно исправлено**: `nginx.local.conf` не содержал маршрут
`/static/*` (round25 это уже отмечал как пробел тестового окружения) —
из-за этого `year_district_json`/`db_matches_static` не могли пройти на
стенде технически. Добавлен `location /static/` (+ volume `./static` в
`docker-compose.local.yml`/`.server.yml`, оба используют этот конфиг
как факт: `docker inspect` подтвердил, что `pikurr_srv_nginx` реально
поднят из `docker-compose.server.yml`, монтирующего тот же
`nginx.local.conf`). Проверено: `healthcheck.py --base-url http://localhost`
на стенде теперь проходит все 5 проверок.

**Подтверждено** полностью.

### B5. Геодезическая площадь

**Сделано**: `area_ha` в `create_assessment_schema.sql` переведён с
`shape_area` (значение из исходного файла) на
`ST_Area(a.geom::geography) / 10000`. Обоснование возврата к
пересчёту — доставка теперь гарантированно вызывает `REFRESH` ровно
один раз (round25, блок B2), цена пересчёта геометрии больше не
умножается на число запросов пользователя, как было в round22, когда
`assessment_ready` был обычным `VIEW`.

**Замер `REFRESH` на VPS** (боевой объём, 59209/55784 строк):

| | прогон 1 | прогон 2 | прогон 3 | прогон 4 | среднее |
|---|---|---|---|---|---|
| до (`shape_area`) | 5.601с | 18.519с | 7.416с | 10.370с | ~10.5с |
| после (`ST_Area(geom::geography)`) | 18.872с | 11.420с | 15.011с | — | ~15.1с |

Прирост среднего ≈ **4.6с** — порог отказа ТЗ (30с) не превышен, метод
внедрён.

**Погрешность прежнего метода** (проверено фактом на всех 59209 строках
`agrifields`): средняя абсолютная относительная погрешность **0.0246%**,
максимум **51.846%**, 2 строки с расхождением >10%. Строка
`nr_user=22490000030364` (round22 отмечал как «явно бракованную», «~100%»)
— по факту **две** строки под этим `nr_user`, `shape_area` даёт одну и
ту же площадь (1.2146 га) для обеих, геодезический метод — разные и
почти вдвое меньшие значения (0.5849 и 0.6296 га, отклонение -51.8% и
-48.2%) — той же природы, что отмечал round22, но не ровно «100%».
После перехода эта строка получает более точное значение — не регресс.

**Применено**: и на VPS, и на стенде (`DROP MATERIALIZED VIEW
assessment_ready CASCADE` + повторный запуск `create_assessment_schema.sql`
— штатный путь `deliver.py`/`recreate_views()` **не покрывает** смену
формулы уже материализованного представления: `CREATE MATERIALIZED
VIEW IF NOT EXISTS` молча пропускает пересоздание, если объект уже
существует, что и произошло при первой попытке — зафиксировано как
находка, не была известна раньше). `healthcheck.py` зелёный на обоих
хостах после миграции (VPS: 4/5 при запуске без `FRONTEND_DB_PASSWORD`
локально — ограничение среды, не дефект; стенд: 5/5).

`docs/data-quality-report.md`, раздел 7 — переписан фактически (метод
изменён, число измерений, судьба конкретной строки), без оценки
пригодности задним числом.

**Подтверждено** полностью.

---

## Что требует решения пользователя

1. **Мастер-пароль GeoServer всё ещё не сменён** (round25, A3, не
   входило в scope этого раунда) — остаётся единственным секретом,
   совпадающим с утёкшим значением на обоих хостах.
2. **`levelsagg` блокируется ~14.6с на каждой доставке на VPS** (B2,
   новая находка) — не исправлялось, решение (перевести на
   `assessment_ready` или отдельный materialized view) за пользователем.
3. **Снимок диска VPS** — сделан пользователем в ходе этого раунда
   (A3), хорошая практика продолжать по расписанию (Console → Compute
   Cloud → Snapshot schedules).
4. **`geoserver_data/` в git устарел** относительно боевого состояния
   (найдено в A3: `datastore.xml` хранит неактуальный формат пароля) —
   не исправлялось (вне заявленного scope раунда, требует отдельной
   синхронизации снапшота с боевым GeoServer).
5. **`deliver.py --cleanup` по-прежнему без расписания** (round25 C6,
   не тронуто в этом раунде).
6. **Мастер-пароль защищает keystore на обоих хостах одинаково слабо**
   — совпадает с выводом round25, актуальность не изменилась.
