# Раунд 31: доказать выигрыш, убрать блокировку при доставке

См. `prompts/PROMPT_verify_win_round31.md`. Оспаривается масштаб
итогового выигрыша в блоке E round30: устранение `DISTINCT ON`
объясняет ~11× по SQL, а итоговая таблица показывала 50-100× —
разница может объясняться тем, что k6-скрипт за это время правился
дважды (round29 C1/C2, round30 zoom/pan/click), не только правкой
схемы.

**Правило неизменного инструмента**: k6-скрипты в этом раунде не
правятся — используются существующие файлы round28/round30 как есть,
копии для доп. измерений — отдельными файлами.

---

## Блок A. Решающий опыт — старый скрипт на сегодняшней VPS

### A.1. Скрипт round28 — diff против round30

Взят `/tmp/round28_k6/realistic_profile.js` (сохранён с round28,
не редактировался с того момента) — это тот самый файл, которым
снимались числа round28's блока B (VPS-часть). Сравнение с
`/tmp/round30_realistic_profile.js` (текущая версия, round30's блок
E):

```diff
--- round28_original.js
+++ round30_fixed.js
@@ page_load @@
-  24 тайла: 12 pikurr:fields_latest (без параметров) + 12 pikurr:fields
-  (с CQL_FILTER=year=2025&time=...)
+  12 тайлов: pikurr:fields_latest (только &time=..., без CQL_FILTER)

@@ zoom_change @@
-  24 тайла (тот же паттерн 12+12)
+  12 тайлов (fields_latest, &time=...)

@@ pan @@
-  8 тайлов pikurr:fields (&CQL_FILTER=year=2025&time=...)
+  8 тайлов pikurr:fields_latest (&time=...)

@@ click_feature (GetFeatureInfo) @@
-  layers=pikurr:fields&query_layers=pikurr:fields
+  layers=pikurr:fields_latest&query_layers=pikurr:fields_latest
```

Полный `diff -u` — в `/tmp/round31_k6/diff_round28_vs_round30.txt`
(приложен построчно выше, по сути).

**Ключевое наблюдение до всякого замера**: `page_load` и
`zoom_change` в round28-скрипте запрашивают **вдвое больше тайлов**
(24 против 12) на каждое действие — это само по себе, независимо от
состояния БД, должно давать ~2× разницы в `action_page_load`/
`action_zoom` между скриптами. Это и предстоит отделить от эффекта
материализации.

### A.2-A.4. Прогоны на сегодняшней VPS (машина: VPS, `geobotany.of.by`)

Оба скрипта — **без единой правки** (правило раунда). VU=1,2,3, по 3
повтора каждый, шаффл-порядок (seed=2035, для первой серии;
последовательность сохранена и для второй серии на тех же VU/run):
`1,2,3,3,2,3,1,1,2` (с точки зрения VU; конкретные повторы каждого VU
выполнены как отдельные `run1/run2/run3`).

**Серия 1 — скрипт round28** (неизменный,
`/tmp/round28_k6/realistic_profile.js`), `action_page_load`:

| VU | run1 | run2 | run3 | медиана | `http_req_failed` |
|---|---|---|---|---|---|
| 1 | 346мс | 374мс | 831мс | **374мс** | 0% на всех 3 |
| 2 | 988.5мс | 1.26с | 797.5мс | **988мс** | 0% на всех 3 |
| 3 | 1.0с | 3.41с | 1.09с | **1.09с** | 0% на всех 3 |

**Серия 2 — скрипт round30** (неизменный,
`/tmp/round30_realistic_profile.js`, 12 тайлов/1 слой), тот же
порядок:

| VU | run1 | run2 | run3 | медиана | `http_req_failed` |
|---|---|---|---|---|---|
| 1 | 307мс | 244мс | 242мс | **244мс** | 0% на всех 3 |
| 2 | 469мс | 385мс | 319мс | **385мс** | 0% на всех 3 |
| 3 | 1.04с | 460мс | 521мс | **521мс** | 0% на всех 3 |

**0% ошибок на всех 18 прогонах** — эскалацию останавливать не
пришлось.

### Разложение общего выигрыша

| VU | round28 (исходное, round28) | round28-скрипт сегодня | round30-скрипт сегодня | материализация+прочее | скрипт | итого |
|---|---|---|---|---|---|---|
| 1 | 19.8с | 374мс | 244мс | **52.9×** | **1.5×** | **81×** |
| 2 | 63с (57-69с) | 988мс | 385мс | **63.7×** | **2.6×** | **164×** |
| 3 | 82.5с (1м9с-1м38с) | 1.09с | 521мс | **75.7×** | **2.1×** | **158×** |

**Вклад изменений скрипта — скромный и предсказуемый** (1.5-2.6×,
близко к ожидаемому ~2× от вдвое меньшего числа тайлов в
`page_load`/`zoom_change`) — согласуется с гипотезой ТЗ о том, что
инструмент дал не всё улучшение.

**Честная, непредвиденная нестыковка**: «доля материализации»
(round28-скрипт на сегодняшней VPS против round28-исходного, **тот
же самый скрипт**, значит разница — только состояние витрины) —
**52.9-75.7×**, что **заметно больше**, чем ~11-14×, установленные
чистым SQL `EXPLAIN` в round30, блок A (236-293мс → 21мс на тайл).
Материализация объясняет часть этого разрыва, но не весь — 75× ≠ 14×.
**Не выдаём одно за другое** — правдоподобные (не проверенные в этом
блоке) причины оставшегося разрыва:
- **Пакетный эффект `http.batch()`**: round28-скрипт отправляет 24
  тайла одним батчем (12 `fields_latest` + 12 `fields`), и МЕДЛЕННЫЕ
  запросы `fields_latest` (тогда — 236-293мс каждый, до материализации)
  могли **удерживать ресурсы** (потоки GeoServer, соединения к
  PostGIS), от которых зависели остальные 12 запросов `fields` в том
  же батче — то есть цена `DISTINCT ON` могла **компенсироваться не
  только на своих тайлах, но и замедлять соседние**, раздувая
  суммарное время всего батча сильнее, чем ×11-14 на один запрос.
- **Разброс между сессиями замера**: round27/28 сами устанавливали
  1×-3× разброс VPS между прогонами как норму (`прерываемая ВМ`,
  burstable vCPU) — часть 52-76× может быть не связана ни с
  материализацией, ни со скриптом, а с тем, что round28-исходные
  числа снимались в другой день/сессии с другим состоянием машины
  (round27/28 сами предупреждали об этом лимите сопоставимости).

**Итоговая формулировка (в требуемом ТЗ формате)**: из общего
улучшения **81-164×** (в зависимости от VU) на долю материализации
`assessment_ready_latest` (плюс сопутствующие эффекты состояния
витрины) приходится **~53-76×**, на долю изменений k6-скрипта
(вдвое меньше тайлов) — **~1.5-2.6×**. Доля материализации **больше**,
чем можно объяснить одним лишь замером `EXPLAIN` (~11-14×) — остаток
(~4-7×) **не объяснён в этом блоке**, зафиксирован честно как
открытый вопрос, не приписан ни одному из двух факторов без
доказательства.

**Подтверждено**: реальный выигрыш от материализации — большой и
доминирующий фактор, не иллюзия скрипта (скрипт дал только 1.5-2.6×,
на порядок меньше общего выигрыша). **Опровергнуто** предположение,
что весь разрыв 50-100× объясняется только сменой скрипта — это не
так, основной вклад — реальная правка БД. **Не решено полностью**:
почему «материализация» на практике дала больше, чем 11-14× по SQL —
требует отдельного расследования (блок B/C ниже частично к этому
подступается).

---

## Блок B. Проверка по содержимому в нагрузочном тесте

### B.1. Инструментация (копии, оригиналы не правились)

Созданы отдельные копии обоих скриптов
(`round28_instrumented.js`, `round30_instrumented.js`) с добавленным
сбором размера тела каждого тайла (`Trend` в байтах, раздельно
вектор/растр) и грубой проверкой "не пустой ответ" —
`byteDiversity()` (число различных байтовых значений в сжатом PNG-
потоке, сэмплировано). Честная оговорка: k6 не имеет декодера PNG —
это прокси, не строгая проверка "растр содержит больше одного
цвета", зафиксировано как таковое в самом коде.

### B.2. Распределение размеров ответов, VU=1 и VU=3 (машина: VPS)

| Скрипт | VU | тайлов проверено | медиана вектор | медиана растр | доля <2КБ |
|---|---|---|---|---|---|
| round28 (неизм.) | 1 | 98 | 1784 байт | 1541 байт | **94%** (92/98) |
| round28 (неизм.) | 3 | 389 | 1784 байт | 1809.5 байт | **58%** (225/389) |
| round30 (неизм.) | 1 | 93 | 1784 байт | 1560 байт | **100%** (93/93) |
| round30 (неизм.) | 3 | 179 | 5379 байт | 6287.5 байт | **42%** (76/179) |

**Существенная доля тайлов — пустые/почти пустые** (1784 байта —
похоже на константный размер полностью прозрачного PNG-тайла без
единого поля внутри bbox). Доля колеблется 42-100% в зависимости от
случайного центра сессии (`randomCenter()` — равномерно по всему
bbox покрытия).

### B.3. Факт о доле реальных данных + переизмерение

**Честная оговорка**: округлить в замерах round30's блока E "сколько
тайлов несли реальные данные" **невозможно ретроактивно** — те
прогоны не сохраняли тела ответов, только тайминги. Ниже — **новый**
замер в тех же условиях (тот же случайный `randomCenter()`), не
ревизия старых данных.

**Проверка гипотезы «неверный bbox»**: реальный охват данных на VPS
(`SELECT ST_Extent(geom) FROM agrifields`) —
`BOX(26.44 54.89, 30.91 56.11)` — **почти совпадает** с диапазоном
скрипта (`LON 26.4-31.0, LAT 54.8-56.2`). **Диапазон не ошибочен** —
проблема в другом: поля **редко** покрывают площадь внутри этого
прямоугольника (леса, вода, другие районы без данных, разрывы между
хозяйствами) — случайная равномерная точка внутри extent часто
попадает мимо любого поля.

**Дополнительная серия** (третий вариант, `round30_realcentroids.js`
— копия `round30_instrumented.js`, `randomCenter()` заменён на выбор
из 200 реальных центроидов полей, полученных `TABLESAMPLE SYSTEM(0.5)`
на VPS):

| VU | тайлов проверено | медиана вектор | медиана растр | доля <2КБ |
|---|---|---|---|---|
| 1 | 68 | 1784 байт | — | 51% (35/68) |
| 3 | 197 | **27905 байт** | 6155.5 байт | **11%** (22/197) |

**Заметное улучшение при VU=3** (доля пустых упала с 42% до 11%,
медиана векторного тайла выросла в ~5× — с 5379 до 27905 байт) —
подтверждает гипотезу: сессии, стартующие с реального центроида поля,
с большей вероятностью остаются рядом с полями при последующих
панорамированиях/зумах. При VU=1 улучшение слабее (51% всё ещё <2КБ)
— один центроид на всю сессию не гарантирует, что ВСЕ 5-10
последующих панов/зумов останутся у полей (сдвиги случайны по
направлению).

**Вывод**: гипотеза о нерепрезентативном покрытии **подтверждена
частично** — исходный bbox-диапазон корректен, но случайное
равномерное сэмплирование внутри него даёт значительную (42-100% в
зависимости от VU) долю пустых тайлов; привязка к реальным
центроидам полей заметно снижает эту долю, но не устраняет
полностью (сессия всё равно блуждает панорамированием). Это означает,
что **числа `action_page_load`/`action_zoom` во всех предыдущих
раундах (28-31) отчасти измеряли не "типичную" нагрузку с полями, а
смесь настоящих полей и пустых областей** — не аннулирует выводы о
относительном ускорении (материализация помогает одинаково что
пустым, что заполненным тайлам — `DISTINCT ON` перед фильтром не
зависит от того, найдёт ли фильтр что-то), но означает, что абсолютные
цифры (медианы в мс) для пустых тайлов **занижают** реальную типичную
нагрузку пользователя, который в основном смотрит именно на поля, не
на случайные точки карты.

**Не внедрялось** — правка bbox-логики оригинальных скриптов (правило
неизменного инструмента); только описано и продемонстрировано
дополнительной, отдельной серией.
