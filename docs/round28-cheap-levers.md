# Раунд 28: дешёвые рычаги до решения об апгрейде ВМ

См. `prompts/PROMPT_cheap_levers_round28.md` и
`prompts/PROMPT_cheap_levers_round28_addendum.md` (порядок:
A6 → A7 → B → C → D → E). Вопрос раунда: нужно ли вообще менять ВМ,
или витрина уложится в текущую при программных правках. Апгрейд ВМ и
изменения на VPS — не выполнялись, все опыты — на эмуляторе
`pikurr-emu`, на VPS — только чтение и замеры.

**Статус этого среза отчёта**: блок A (весь, включая дополнение A6/A7),
рычаг 1 блока C (обзоры), блок E (первая доставка на пустую БД) и
эмуляторная часть блока B (реалистичный профиль нагрузки) —
выполнены. VPS-часть блока B, остальное блока C (рычаги 2-5, 7) и
блок D — отдельным продолжением. Ответ на вопрос раунда целиком по
этому срезу **не даётся** — ТЗ прямо требует опираться на A-D вместе.

**Важная коррекция по ходу работы**: гипотеза из первого среза блока A
(«control-flow — причина 503») **опровергнута фактом** в A6 —
настоящая причина оказалась другой (кэш браузера, не сервер). См. A6
ниже.

---

## Блок A. Разведка (без изменений)

### A1. Обзоры в растрах

**Сделано**: `gdalinfo` на 5 файлах мозаики на VPS (`N-35-22-V-v-2/3/4`,
`N-35-23-A-a-2/3`), проверка `indexer.properties`/`*.properties`
мозаики, параметров чтения coverage `image_assessment` в GeoServer
(REST), прямое измерение через `/proc/<pid>/io` (байты) и `strace -f`
(файлы) на **эмуляторе** при одном запросе широкого тайла (2°×1°,
256×256px, layer `image_assessment`).

**Находки**:
- Все 5 проверенных TIF: `Size 6144×7168` (или `6144×6912`), сжатие
  `LZW`, **`Block=6144x1`** — полосовая организация (весь ряд пикселей
  в одном блоке), **не тайлированы**. **`Overviews` не встречается ни
  разу** — пирамиды обзоров нет вообще.
- `2025.properties` мозаики: `LevelsNum=1` — подтверждает: у мозаики
  как целого тоже только один (полное разрешение) уровень пирамиды.
- GeoServer-параметры чтения coverage: `USE_JAI_IMAGEREAD=true`,
  `AllowMultithreading=false`, `SUGGESTED_TILE_SIZE=512,512` — но при
  нетайлированных исходниках "подсказка" размера тайла чтения не может
  обойти полосовую организацию файла.
- **Один запрос широкого тайла (машина: эмулятор) открывает ~230
  разных TIF-файлов**, но суммарно читает всего **~668 КБ** (`/proc/PID/io`,
  `read_bytes` до/после: `1356185600`→`1356869632`). В среднем ~2.9 КБ
  на файл — то есть цена не в объёме байт, а в **числе операций
  открытия/поиска** файлов при отсутствии пирамиды: на низком зуме
  запрос всё равно трогает каждый полноразрешающий файл в охвате по
  отдельности, вместо одного уменьшенного обзорного уровня.

**Подтверждено** полностью: обзоров нет, тайлинг файлов отсутствует,
цена одного широкого тайла — не байты, а количество открываемых
файлов (IOPS), что напрямую связывает эту находку с диагнозом round27
(диск/IOPS — первое узкое место).

### A2. Растры против файлового кэша

**Сделано** (машина: VPS, только чтение):
- `du -sh` каталога мозаики: **1.3 ГБ** (912 файлов).
- `free -m` в состоянии покоя: `total=5924 used=3714 free=897
  buff/cache=2034`, **swap уже используется: 190 МБ** (не 0 — даже
  без активной нагрузки в момент замера).
- `docker stats --no-stream`: `geoserver 2.697GiB/3GiB` (RSS почти у
  самого потолка контейнерного лимита), `postgis 565MiB/1.5GiB`,
  `react 10.9MiB/128MiB`, `caddy 57.9MiB/256MiB`.
- Резидентность файлов мозаики в кэше ОС — `vmtouch`/`fincore` на VPS
  не установлены, а установка пакетов на VPS не входит в разрешённое
  «только чтение»; вместо этого — самодостаточный скрипт на `mincore()`
  через `ctypes` (ничего не устанавливает, использует только уже
  имеющийся `python3`), по всем 912 файлам мозаики: **резидентно 25.3%
  страниц (82329/325406) ≈ 337 МБ из 1331 МБ**.

**Вывод**: мозаика (1.3 ГБ) физически меньше, чем `buff/cache`
(2.0 ГБ) в моменте, но резидентно в кэше — только четверть её объёма.
Что вытесняет: GeoServer занимает 2.7 из 3 ГБ лимита контейнера почти
целиком (heap 2048M + off-heap/native буферы), плюс PostGIS 565 МБ —
на файловый кэш ОС для растров остаётся мало места на хосте с 5.9 ГБ
общей RAM, и своп уже используется даже в покое. Мозаика **не
помещается в доступный кэш с комфортным запасом**.

**Подтверждено** полностью.

### A3. Как фронтенд реально запрашивает тайлы

**Сделано**: перехват реальных сетевых запросов в браузере (Chrome,
`claude-in-chrome`, не буквально Firefox — тот же результат) на живой
витрине `https://geobotany.of.by`, три сценария: загрузка страницы,
сдвиг карты (drag), включение слоя «AI оценка».

**Находки**:
- **GWC не используется нигде** — все запросы идут напрямую
  `.../geoserver/pikurr/wms?...GetMap...`, нет `/gwc/service/wms`, нет
  параметра `tiled=true`. Подтверждает round25/26/27: слои не
  зарегистрированы в GWC, каждый тайл — полный рендер, не кэш-хит.
- Сетка — стандартная Leaflet, тайл 256×256px, EPSG:3857.
- **Загрузка страницы (один пользователь, без единого действия)**
  запрашивает **24 тайла одновременно** — по 12 на каждый из двух
  векторных слоёв (`pikurr:fields_latest` и `pikurr:fields`,
  ОБА сразу, не один в зависимости от режима — избыточность, не была
  явно нужна пользователю в этот момент).
- Один `drag` (сдвиг карты на том же зуме) — **8 новых тайлов**
  (только для вскрывшейся области, Leaflet кеширует уже загруженные;
  запрошен только один слой — `fields`).
- Включение переключателя «AI оценка» — **16 тайлов растра
  `image_assessment`** + 1 `GetLegendGraphic`, тоже напрямую WMS, не
  через GWC.
- **Критичная живая находка, не гипотеза**: при загрузке страницы **6
  из 12 запросов к `pikurr:fields_latest` вернули HTTP 503** прямо
  сейчас, в бою (2026-09-22, во время этой проверки). Повторный прямой
  запрос той же комбинации (curl) сразу после — `200`, тело валидный
  PNG: сбой не постоянный, а именно от одновременного всплеска.
  Причина найдена фактом — `~/repikurr/geoserver_data/controlflow.properties`
  на VPS:
  ```
  timeout=60
  ows.global=100
  ows.wms.getmap=10
  ows.wfs.getfeature.application/msexcel=4
  user=6
  ows.gwc=16
  user.ows.wps.execute=1000/d;30s
  ```
  **`user=6`** (не более 6 одновременных запросов на пользователя) и
  **`ows.wms.getmap=10`** (глобально) — а одна загрузка страницы уже
  создаёт 24 одновременных WMS GetMap-запроса от одного пользователя.
  Это **не гипотеза о нагрузочном тесте «тяжелее реальной»** — это
  подтверждённый фактом баг конфигурации, который ломает **самый
  обычный, однопользовательский** визит на сайт, безо всякой
  синтетической нагрузки.

**Важный практический вывод**: это отдельный, лёгкий рычаг —
поднять `user`/`ows.wms.getmap` в `controlflow.properties` — не
входил в исходный список рычагов ТЗ (обзоры/куча/GC-потоки/GWC-засев),
но найден фактом при честной разведке A3 и, вероятно, даёт более
быстрый и надёжный выигрыш, чем часть исходных пяти. Кандидат для
проверки в блоке C (не изменялось на VPS — только диагностика).

**Побочная находка**: `pikurr:fields_latest` и `pikurr:fields`
запрашиваются ОБА при каждой загрузке страницы, хотя в интерфейсе
виден только один режим года за раз — потенциальное удвоение
начальной тайловой нагрузки. Не проверялось, чем это вызвано во
фронтенд-коде (фронтенд менять запрещено, но само наблюдение стоит
знать).

**Подтверждено** полностью.

### A4. Параметры JVM

**Сделано**: поиск источника `ParallelGCThreads=20 -XX:ConcGCThreads=5`
в `entrypoint.sh` образа `kartoza/geoserver` (машина: VPS, `docker exec
... grep`, только чтение).

**Находка**: значения **жёстко зашиты литералами** в скрипте
(`-XX:ParallelGCThreads=20 -XX:ConcGCThreads=5` — не переменные
окружения, не производные от числа ядер), собираются в `GEOSERVER_OPTS`
и попадают в `JAVA_OPTS` ДО `${ADDITIONAL_JAVA_STARTUP_OPTIONS}`,
который подставляется последним в командной строке. Значит:
переопределение через `ADDITIONAL_JAVA_STARTUP_OPTIONS` (тот же
механизм, что включил GC-лог в round27, B2) технически работает — JVM
берёт последнее значение `-XX:ParallelGCThreads=N`/`ConcGCThreads=N`
в командной строке. Не менялось на VPS (запрещено ТЗ), только
установлен факт механизма для блока C.

**Подтверждено** полностью.

### A5. GC-лог

**Сделано**: скачан накопленный с round27 (B2) лог
(`~/repikurr/geoserver_data/gc.log`, машина: VPS), разбор пауз по
регулярным выражениям.

**Результат** (окно лога: `2026-09-21T11:02:52` — `2026-09-21T18:36:43`,
~7.5 часов, 660 GC-событий):
- Всего распознано пауз: **756**.
- Медиана: **45.1мс**, среднее: **61.4мс**, максимум: **4166.1мс**.
- **Пауз >1с: 3** — `12:02:44` (4166.1мс, `Young (Mixed)`),
  `12:17:30` (1170.0мс, `Young (Prepare Mixed)`), `12:20:22` (3474.4мс,
  `Young (Concurrent Start)`).
- Full GC: 1 раз, `11:49:47`, 713.9мс — не катастрофично.

**Сопоставление с нагрузкой**: round27 B5 (реальная доставка с
одновременной нагрузкой k6 1→5→10 VU) шла с `12:13:53` по `12:21:12` —
**обе паузы `12:17:30` и `12:20:22` попадают точно внутрь этого окна**.
Третья (`12:02:44`, самая длинная) на ~11 минут раньше — по времени
согласуется с завершающими, самыми тяжёлыми ступенями лестницы round27
B4 (20/40 VU), которые шли непосредственно перед B5. Отдельных
сохранённых `iostat`/`%steal` с точной привязкой к этим трём меткам
времени нет (round27 не вёл непрерывный `vmstat` через всю сессию, а
только на отдельных ступенях) — сопоставление по совпадению временных
окон, не по одновременному прямому измерению `%steal` секунда-в-секунду.

**Вывод**: длинные паузы **не фоновые, беспричинные события** — все
три происходят строго во время известных эпизодов реальной нагрузки
(round27 B4/B5), не в покое. Это говорит в пользу гипотезы «GC-паузы —
следствие той же нагрузки на диск/память, что уже диагностирована»,
а не независимая, отдельная проблема JVM, но не исключает вклад GC
как усиливающего фактора (мусор от рендеринга большого числа тайлов —
дополнительное давление на heap именно в моменты нагрузки).

**Подтверждено частично**: число/максимум/паузы>1с — установлены
фактом; точное сопоставление секунда-в-секунду с `%steal`/`iostat` —
не выполнено (данных нет), только сопоставление по известным временным
окнам активности.

### A6. Причина HTTP 503 на загрузке страницы — доказать, не угадать

**Задача ТЗ явно предупреждала**: «гипотеза controlflow.properties не
объясняет асимметрию» (503 только у `fields_latest`, никогда у
`fields`) — и оказалась права.

**A6.1 (VPS, только чтение)**: момент вчерашнего сбоя (2026-09-22,
~07:13-07:14 UTC) — лог GeoServer за это окно не содержит НИ ОДНОЙ
ошибки/исключения (`grep -iE 'error|exception|SEVERE'` — пусто),
`geoserver.flow` показывает пик **3 одновременных запроса**, ни разу
не подошёл к лимиту `user=6`. Лог Caddy — 0 строк за то же окно
(в `Caddyfile` вообще нет директивы `log`, доступа не видно).
Поскольку VPS в момент продолжения работы над ТЗ была недоступна
(прерываемая ВМ была остановлена, `curl`/`ssh` — таймаут; поднялась
позже сама, без вмешательства с моей стороны — доступа к Yandex Cloud
API у меня нет), лог за сам момент из блока A восстановить не
получилось; сработал прописанный в ТЗ запасной путь — повторная
загрузка страницы вживую.

**A6.1, запасной путь — повтор вживую (VPS, только чтение +
диагностика в браузере)**: страница перезагружена дважды. **Оба раза
— 100% воспроизводимо**: все (17/17, затем снова 17/17) запросы
`fields_latest` — `503`, все запросы `fields` — `200`. Одновременно
писался живой `docker logs -f` GeoServer — **за всё окно перезагрузки
(07:39:00-07:39:05) не записано НИ ОДНОЙ строки**, ни для
`fields_latest`, ни для `fields`.

**Решающий тест**: через `performance.getEntriesByType('resource')` —
у всех 503-ответов `fields_latest` **`transferSize: 0`** (в Resource
Timing API это означает «отдано из кэша браузера, сеть не
использовалась» — то же самое объясняет отсутствие строк в логе
GeoServer). Прямой `fetch(url, {cache: "no-store"})` на **тот же самый
URL** — **`200`**, валидный PNG. Обычный hard-reload (`Ctrl+Shift+R`)
кэш не сбросил (он бьёт по документу, не по XHR/Image-запросам,
которые Leaflet шлёт уже после загрузки).

**Настоящая причина найдена и доказана**: `pikurr:fields_latest`
запрашивается **без cache-buster** — URL идентичен от загрузки к
загрузке. `pikurr:fields`, наоборот, всегда содержит `&time=<epoch>` —
уникальный при каждой загрузке. Если хотя бы **один раз в прошлом**
(на любой машине, в любой сессии) запрос к этому — единственному,
неизменному — URL `fields_latest` получил `503` (реальный, но
разовый/случайный сбой — сеть, перезапуск контейнера, что угодно), и
этот ответ по какой-то причине попал в HTTP-кэш браузера, то **с этого
момента и навсегда** для этого конкретного браузера/машины все
последующие загрузки страницы будут молча получать тот же самый
закэшированный `503`, независимо от того, что реальный сервер прямо
сейчас отвечает нормально. Точная причина, почему браузер вообще
закэшировал ответ с кодом `503` (по HTTP-спецификации `503` не входит
в список кодов, кэшируемых эвристически без явных заголовков) — не
установлена: исторический ответ, который попал в кэш, недоступен для
осмотра задним числом; `Caddyfile` не выставляет `Cache-Control` на
пути `/geoserver/*` вообще (проверено — только `/static/*` имеет
`Cache-Control: public, max-age=300`), значит источник заголовка, если
он был, — сам GeoServer в момент того давнего сбоя, не Caddy.

**A6.2 (VPS, только чтение, без пароля)** — параметры пула соединений
датастора `postgis_pikurr` (для полноты, хотя они и не оказались
причиной): `min connections=1`, `max connections=10`,
`Connection timeout=20` (с), `fetch size=1000`,
`Max connection idle time=300` (с). Стандартные значения GeoTools JDBC
Pool, не изменялись.

**A6.3-5 — не выполнялись**: пункты ТЗ про воспроизведение на
эмуляторе с подогнанными `controlflow.properties`/параметрами
датастора и подбор, какой из них «лечит» 503, **неприменимы** —
реальная причина не серверная (концепция «снять ограничение
`user`/`ows.wms.getmap`/`max connections`» не имеет отношения к
кэшу браузера конкретного посетителя). **Рычаг 6 в блок C не
добавляется** — предмета для него не оказалось.

**Практический вывод, важнее гипотезы про control-flow**: у **любого**
реального посетителя сайта, чей браузер хоть раз получил `503` на
`fields_latest` (по любой причине, включая кратковременный сбой самой
VPS, round27/28 которых было несколько), карта в режиме «все годы»
сломана **навсегда, молча, без ошибки на экране**, пока пользователь
сам не очистит кэш браузера — потому что URL никогда не меняется.
Исправление — программное (не апгрейд ВМ): добавить `fields_latest`
тот же cache-buster `&time=<epoch>`, что уже есть у `fields`
(фронтенд менять в этом раунде запрещено — только описать), либо
чтобы GeoServer/Caddy отдавали `Cache-Control: no-store` на все
динамические WMS/WFS-ответы (конфигурация Caddy — можно проверить на
эмуляторе, применение на VPS — решение пользователя, вне scope этого
раунда).

**Подтверждено полностью, с честной коррекцией собственной более
ранней гипотезы** (не угадано — доказано фактом, ровно как требовало
ТЗ).

### A7. Физические и логические чтения на широком тайле

**Сделано** (машина: эмулятор): тот же запрос широкого тайла, что и
A1, с `/proc/PID/io` (`rchar` — логический объём, `read_bytes` —
физический), в двух состояниях — сразу после
`echo 3 > /proc/sys/vm/drop_caches` и повторно (тёплый кэш).

| | Холодный | Тёплый |
|---|---|---|
| Время | 2.569с | 0.208с (**в 12 раз быстрее**) |
| `rchar` (логически) | +43.9 МБ | +43.9 МБ (**идентично**) |
| `read_bytes` (физически) | +75.2 МБ | **+0 МБ** |
| `openat` | 266 | — |

**Вывод, строго по числам**: логический объём распаковки LZW-полос
**не зависит от состояния кэша** — GeoServer каждый раз декодирует
одни и те же ~44 МБ данных (это и есть цена отсутствия тайлирования/
обзоров из A1 — распаковка целых полос шире, чем нужно окну). Разница
в 12× между холодным и тёплым замером **целиком** объясняется
устранением физического чтения с диска (75 МБ → 0). Значит: цена
широкого тайла — **и в физических чтениях** (когда кэш холодный или
вытеснен — привязано к A2), **и отдельно в самой распаковке**
(0.21с CPU держится даже при полностью тёплом кэше, обзоры устранили
бы и это). Одно не отменяет другое.

**Подтверждено** полностью.

---

## Блок C (частично). Рычаг 1 — обзоры, уточнение метода

**Сделано** (машина: эмулятор, 5 файлов мозаики): данные категориальные
(`gdalinfo -hist` — несколько дискретных значений 0-6, не непрерывный
тон) → для обзоров нужен метод **nearest**, не `average` (иначе
появятся несуществующие «смешанные» классы).

**(а) `gdaladdo -r nearest`, уровни 2/4/8/16, внешние `.ovr`**:
- Без сжатия по умолчанию `.ovr` оказался **больше** исходника
  (14.6 МБ против ~1 МБ на файл) — честно зафиксировано и исправлено:
  `--config COMPRESS_OVERVIEW LZW` (то же сжатие, что у исходников).
- С LZW: 0.089с/файл, средний `.ovr` — 223 КБ.
- Экстраполяция на 912 файлов: **~81с** построения, **+208 МБ** к
  пакету, **+51с** к передаче по каналу стенд→VPS (4.1 МБ/с, round27
  B3).

**(б) Пересборка в COG** (`gdal_translate -of COG -co COMPRESS=LZW
-co RESAMPLING=NEAREST -co BLOCKSIZE=512`) — выбран COG, а не
раздельные `gdal_translate -co TILED=YES` + `gdaladdo`, потому что
современный драйвер COG делает тайлинг+сжатие+обзоры одной командой,
без риска рассинхронизации параметров между двумя вызовами:
- 0.234с/файл — на 912 файлов **~213с (3.6 мин)**.
- Checksum пикселей до/после (`gdalinfo -checksum`) — **идентичен**
  (54101=54101), потеря данных исключена (LZW без потерь).
- **Неожиданный результат**: тайлированный+пересжатый COG оказался
  **в 3.7 раза МЕНЬШЕ** исходника (27.1% от размера), несмотря на то,
  что включает и полное разрешение, и 4 уровня обзоров — тайлы 512×512
  сжимаются LZW заметно эффективнее, чем полноширинные полосы 6144×1
  для этих категориальных данных.
- Экстраполяция на всю мозаику (1.3 ГБ): **~353 МБ** (экономия
  **~947 МБ**), передача по каналу стенд→VPS — на **~231с БЫСТРЕЕ**,
  не медленнее.

**Проверка фактом — видит ли ImageMosaic новые уровни без
переиндексации**: один файл в живой мозаике эмулятора подменён на COG-
версию (с бэкапом оригинала). Простой `/rest/.../reset` —
`LevelsNum` в `2025.properties` остался **1** (не подхватил). Тот же
цикл, что `deliver.py` выполняет на КАЖДОЙ обычной доставке (`reset` →
удаление файлов индекса года → `external.imagemosaic` harvest →
`reset`) — **`LevelsNum` стал 5**. Оригинал восстановлен, мозаика
возвращена в исходное состояние, `healthcheck` зелёный.

**Вывод**: переиндексация нужна, но **уже происходит автоматически на
каждой обычной доставке** — добавление обзоров не требует правок
пайплайна `deliver.py`/`reload_geoserver()`.

**Где строить обзоры**: только в ETL на стенде (16 ядер, дисковых
ограничений нет), **не** на VPS при доставке — 213с работы `gdal_translate`
на 2 vCPU и уже перегруженном диске VPS (round27, диагноз) добавили бы
реальную дополнительную нагрузку именно туда, где её меньше всего
нужно. Не внедрялось в ETL — только описано, как и требовало ТЗ.

**Подтверждено** полностью: вариант (б), COG, предпочтителен варианту
(а) практически по всем осям — меньше место на диске (не больше),
тайлирование заодно с обзорами, единственный минус — дольше строить
(213с против 81с на всю мозаику, но это разовая операция в ETL, не на
критичном пути доставки).

---

## Блок E — доставка на пустую БД («первая доставка») без ручных вмешательств

Цель: `deliver.py` + SQL-схема должны сами доводить полностью пустую БД
до рабочего состояния при первой доставке, без ручных
`CREATE TABLE ... LIKE`, которыми это приходилось обходить вручную на
эмуляторе в round27/28. Проверка — фактом: снос БД эмулятора «под
ноль», применение только документированных шагов, одна реальная
доставка, healthcheck.

**Правки (все — в `REPIKURR/deliver.py` и `PIKURR/src/sqlscripts/`,
математика/схема ETL-контура не менялись):**

1. **`_swap_staged_tables()`**: раньше падал с «relation does not
   exist», если боевой таблицы ещё нет (первая доставка). Теперь для
   отсутствующих `agrifields`/`razgrafka` создаёт боевую таблицу
   `CREATE TABLE ... (LIKE ..._stage INCLUDING ALL)` внутри той же
   транзакции, что и перенос данных; проверка на «усыхание» (round26,
   B1) для таких слоёв пропускается (сравнивать не с чем).
2. **`INSERT INTO {layer} SELECT * FROM {layer}_stage`** (позиционный)
   заменён на явный список колонок `_stage` — раньше падал на
   `assessment`, у которого боевая таблица содержит лишнюю колонку
   `valuation`, отсутствующую у `ogr2ogr`.
3. **Найдено фактом при первом реальном прогоне (не было видно по
   коду до живого теста)**: `LIKE ... INCLUDING ALL` копирует
   DEFAULT-выражение колонки `ogc_fid` (`SERIAL`) дословно —
   `nextval('agrifields_stage_ogc_fid_seq')`. Боевая таблица
   оставалась привязанной к последовательности `_stage`-таблицы, и
   последующий `DROP TABLE agrifields_stage` в конце той же транзакции
   падал («cannot drop table ... because other objects depend on
   it»). Исправлено: после `CREATE TABLE LIKE` такие колонки
   перепривязываются на свежесозданную, «родную» последовательность
   (`deliver.py`, `_swap_staged_tables()`).
4. **Найдено фактом**: `ensure_assessment_schema()` безусловно делал
   `DROP MATERIALIZED VIEW IF EXISTS assessment_ready CASCADE` — после
   `bootstrap_empty_schema.sql` этот объект является обычным `VIEW`
   (заглушка), и `IF EXISTS` не спасает при несовпадении *типа*
   объекта (`ERROR: "assessment_ready" is not a materialized view`).
   Исправлено по образцу самого `bootstrap_empty_schema.sql`: дропаются
   оба варианта (`DROP VIEW IF EXISTS ...; DROP MATERIALIZED VIEW IF
   EXISTS ...;`) перед пересозданием.
5. **`create_assessment_schema.sql`**: таблица `assessment` приведена к
   реальной структуре продакшна (`fid` — PK, `id`, `stats`/`description`
   как `VARCHAR`, `updated_at` — `TIMESTAMPTZ`; было ошибочно
   `id SERIAL PRIMARY KEY`/`JSONB`/`TIMESTAMP` — не совпадало с тем,
   что реально создаёт `ogr2ogr`). Добавлены `CREATE TABLE IF NOT
   EXISTS` для `agrifields`/`razgrafka` — с той же реальной структурой,
   на случай, когда `_swap_staged_tables()` их сама не создаст (путь
   `staged=False`, отладочный).
6. **`bootstrap_empty_schema.sql`**: та же правка структуры `assessment`
   — этот скрипт выполняется **раньше** `create_assessment_schema.sql`
   на чистом инстансе, и его `CREATE TABLE IF NOT EXISTS` создавал бы
   таблицу со старой (неверной) структурой, после чего `IF NOT EXISTS`
   в основном скрипте молча пропустил бы создание с исправленной.

**Проверка фактом (эмулятор `pikurr-emu`, снос PostGIS-тома → чистый
`postgis` → `bootstrap_empty_schema.sql` → `geoserver` → одна реальная
доставка, без единого ручного `CREATE TABLE ... LIKE`):**

Тестовым пакетом взят не старый `outputs/dist/*.zip` (в них слой в
`vectors.gpkg` называется `fields_assessment` — устаревшее именование,
несовместимое с текущим `deliver.py`, который жёстко ждёт слои
`agrifields`/`razgrafka`/`assessment`), а свежий пакет, собранный
`PackageTask().run()` напрямую в ETL-контейнере стенда из реальных
текущих данных (`pikurr_update_2025_2026-09-22_09-04.zip`, слои
`agrifields`/`razgrafka`/`assessment` — совпадают с ожиданиями
`deliver.py`).

Побочная находка при подготовке пакета: `create_assessment_schema.sql`
**внутри пакета, собранного на стенде**, — версия от 17 сентября, без
`levelsagg_ready` (round27) и без правок этого блока: деплой на стенде
(`~/pikurr-system`) отстаёт от текущего состояния репозитория. Это
отдельный вопрос синхронизации кода на стенде, не относится к
корректности самих правок — для проверки фактом файл внутри пакета был
подменён на актуальный локальный (иначе проверялась бы не текущая
версия кода). **Требует решения пользователя**: обновить `~/pikurr-system`
на стенде до актуального состояния репозитория.

Итог прогона после всех правок: `agrifields`/`razgrafka` созданы
корректно (собственные, а не унаследованные от `_stage`,
последовательности `ogc_fid`), `assessment` — верной структуры,
перенос `_stage → боевые` прошёл без ошибок, `assessment_ready`/
`levelsagg_ready` пересозданы (`schema_version=2`), `REFRESH
MATERIALIZED VIEW` прошёл, GeoServer-стор растров переиндексирован
(202 файла), встроенный `healthcheck()` в `deliver.py` — «всё в
порядке», статус доставки — `success`.

Отдельный прогон `REPIKURR/healthcheck.py` против
`http://localhost:8090` (эмулятор): `wms_getmap` и `wfs_getfeature` —
`OK` (это то, что реально проверяет результат сегодняшней доставки);
`main_page`/`year_district_json`/`db_matches_static` — `FAIL`, но
ожидаемо: на эмуляторе, как и в round27/28 ранее, развёрнуты только
`postgis`+`geoserver` (без `react-client`/Caddy — образ `repikurr-react`
недоступен на эмуляторе), эти три проверки требуют веб-фронтенда и не
относятся к предмету блока E. Полностью зелёный `healthcheck.py`
возможен только на стенде/VPS с полным стеком.

**Не выполнено в этом срезе**: обновление
`docs/deploy-repikurr-vps.md` шагами, накопленными за rounds 27-28
(`chown -R 1000:1000 geoserver_data`, `GEOSERVER_CONTAINER=<container>`
для не-стендовых хостов) — отдельным шагом продолжения.

---

## Блок B — реалистичный профиль нагрузки (k6), эмулятор

Профиль (`/tmp/round28_k6/realistic_profile.js`, не коммитился —
scratch-скрипт раунда, как и прежние k6-скрипты) построен по перехвату
блока A (`claude-in-chrome`, реальный браузер на geobotany.of.by), а
не по случайным bbox (как в round27) — это отдельный, более
реалистичный сценарий: 24 тайла (`fields_latest`+`fields`) при
загрузке страницы, паны/зумы с реалистичными весами (детальная работа
с полями z=12-15 весит больше, чем обзорные z=7-8), редкие клики
(`GetFeatureInfo`) и включение растровой оценки (`image_assessment` +
легенда). Каждая VU — независимая "сессия" одного пользователя с
паузами 3-10с между действиями (не rps-бомбардировка).

Порядок прогонов на эмуляторе — шаффл (seed=2028, не по возрастанию
VU): `1, 3, 5, 5, 2, 2, 3, 1, 5, 1, 3, 2` → по 3 повтора на каждый
уровень VU (1/2/3/5 одновременных сессий), `--iterations N` = N (по
одной сессии на VU). Данные на эмуляторе — реальная доставка блока E
(не синтетика), кэш ОС/GeoServer не сбрасывался между прогонами
(тёплое состояние — так же ведёт себя продакшн между доставками).

**Результат (эмулятор, машина `pikurr-emu`, 2 vCPU эмуляции VPS —
цифры НЕ переносить на 192.168.251.190 или geobotany.of.by):**

| VU | `action_page_load` (медианы 3 прогонов) | `action_zoom` (медианы) | `session_duration` (медианы) | `errors_total` |
|----|------------------------------------------|---------------------------|----------------------------------|-----------------|
| 1  | 9.3–10.8с | 6.3–11.4с | 57с–1м46с | 0 |
| 2  | 41.3–42.0с | 11–24с | 2м0с–2м27с | 0 |
| 3  | 58с–1м4с | 23–46с | 2м38с–3м36с | 0 |
| 5  | 1м32с–1м47с | 52–58с | 4м21с–5м48с | **2, 4** (двух прогонов из трёх) |

**Наблюдения:**

- `action_page_load` растёт быстрее, чем линейно, с числом
  одновременных VU: 1→2 VU — рост ×4 (10с→41с), 2→3 VU — ещё ×1.4-1.5,
  3→5 VU — ещё ×1.5-1.8. Правдоподобное объяснение — 24 тайла на
  загрузку страницы одной сессии уже насыщают ограниченный процессор
  эмулятора (2 vCPU), и при нескольких одновременных сессиях запросы
  на декомпрессию/рендер тайлов конкурируют за то же CPU — согласуется
  с A1/A7 (нетайлированная мозаика без обзоров требует полной
  декомпрессии ~44МБ на каждый широкий тайл, что уже дорого на одну
  сессию).
- **Ошибки появились только при VU=5** (2 из 5 сессий, 4 из 5 сессий
  на прогонах 2 и 3 из 3) и отсутствовали при VU=1/2/3 — качественный,
  не постепенный порог деградации где-то между 3 и 5 одновременными
  пользователями на этой эмуляции VPS. При VU=1-3 запросы просто
  выстраивались в очередь и отрабатывали (медленнее, но без ошибок).
- `session_duration` при VU=5 доходит до ~6 минут за одну сессию (при
  паузах между действиями 3-10с и 5-10 действий) — то есть бОльшая
  часть этого времени уходит именно на ожидание тайлов, а не на паузы
  пользователя.

**VPS-часть блока B (реальная витрина geobotany.of.by, порядок
`3, 2, 5, 5, 2, 1, 3, 3, 5, 2, 1, 1`)** — отдельным продолжением, см.
ниже.

---

## Что требует решения пользователя (по срезу блока A)

1. **Живой продакшн-баг, причина установлена в A6 (не control-flow —
   исходная гипотеза A3 опровергнута фактом)**: `pikurr:fields_latest`
   запрашивается фронтендом без cache-buster — один случайный `503` в
   прошлом на любой машине навсегда «залипает» в кэше именно этого
   браузера для всех последующих визитов, молча ломая режим «все годы»
   без видимой ошибки. Два возможных исправления, оба не выполнялись
   (вне scope): (а) добавить фронтенду тот же `&time=<epoch>`, что уже
   есть у `fields` — правка фронтенда, запрещена в этом раунде; (б)
   `Cache-Control: no-store` на динамические ответы GeoServer/Caddy —
   конфигурация, можно проверить на эмуляторе в продолжении, применение
   на VPS — решение пользователя.
2. **`fields_latest` и `fields` запрашиваются оба при каждой загрузке
   страницы** (A3, побочная находка) — потенциальное удвоение
   начальной нагрузки; сюда же — рычаг 7 из дополнения ТЗ (запрашивать
   только один слой) намечен, но не выполнен в этом срезе. Разбор
   причины и правка — во фронтенд-коде, запрещена в этом раунде.
3. **Обзоры растров — рычаг 1 выполнен**: COG-пересборка (не просто
   `gdaladdo`) даёт обзоры и тайлинг, да ещё и **сокращает** объём
   мозаики почти в 4 раза — явный кандидат, где строить (ETL на
   стенде) уже определено. Осталось решение пользователя: внедрять ли
   в реальный конвейер (в этом раунде — только описано, не внедрялось).
4. **Блок E выполнен** (см. раздел выше) — три реальных бага в
   `deliver.py`/SQL-схеме первой доставки найдены фактом и исправлены,
   проверено на эмуляторе с нуля. Отдельно — **деплой на стенде
   отстаёт от репозитория** (см. блок E): нужно решение пользователя,
   обновлять ли `~/pikurr-system`. **Блок B (частично начат), D, и
   рычаги 2-5/7 блока C — не выполнены**, отдельное продолжение.
5. **GC-паузы >1с сопоставлены с нагрузкой только по временным окнам**,
   не прямым одновременным измерением `%steal`/`iostat` — если нужна
   более строгая проверка причинности, это отдельная задача (вести
   `vmstat`/`iostat` непрерывно вместе с GC-логом на будущее).
6. **VPS оказалась недоступна во время работы над A6** (прерываемая ВМ
   была остановлена, поднялась сама позже без моего вмешательства — у
   меня нет доступа к Yandex Cloud API/консоли) — стоит проверить,
   действительно ли настроенный в round22-23 автозапуск
   (`cloud-function-autostart`) сейчас работает по факту (шаг 6 его
   README, «проверка фактом» — не переподтверждалась с round23).
